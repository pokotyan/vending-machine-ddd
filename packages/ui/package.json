{
  "name": "ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "tsc --build --clean && tsc --build --force --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vending-machine": "1.0.0",
    "@types/node": "^10.12.0",
    "inversify": "^4.13.0",
    "inversify-inject-decorators": "^3.1.0",
    "reflect-metadata": "^0.1.12",
    "rimraf": "^2.6.2"
  }
}
